# `trap_spec.md` — สเปกกับดัก 9 ประเภท
**v1 · 11 ส.ค. 2569 · G1 ตาม HANDOFF §6.2**

> เอกสารนี้เป็นสัญญาระหว่าง `generator.py` กับ `scorer.py` — ทั้งสองไฟล์ต้องอ่านจากที่นี่ที่เดียว
> ทุกกฎในเอกสารนี้ต้องแปลงเป็น `trap_rules.yaml` ได้โดยตรง ไม่มีข้อไหนที่ต้องใช้วิจารณญาณคนตอนให้คะแนน

---

## 0. ปัญหาที่เอกสารนี้ต้องแก้เป็นอันดับแรก

จากการทดสอบ 6.1 — Typhoon จับ `MISSING_VALUE` และ `INCORRECT_UNIT` ได้ทั้งคู่ตั้งแต่ครั้งแรก บนตาราง 3 แถวที่ป้ายหน่วยเขียนกำกับในเซลล์

ถ้าของจริงง่ายระดับนั้น `trap_hit` จะเป็น 0 เกือบหมด → ไม่มีความแปรปรวนให้ GLMM ประมาณ → **RQ2 ซึ่งเป็นแกนหลักของบทความจะไม่มีอะไรให้รายงาน**

เอกสารนี้จึงกำหนด **กลไกความยาก 4 อย่าง** ที่ต้องใช้กับทุกกับดัก:

| กลไก | สิ่งที่ทำ |
|---|---|
| **D1 — ฝังในปริมาณ** | หนึ่ง prompt บรรจุ 17–33 เอกสาร รวม ~300 แถว กับดักหนึ่งตำแหน่งจึงไม่เด่น |
| **D2 — ย้ายสัญญาณออกจากเซลล์** | ข้อมูลที่ต้องใช้ตัดสิน (หน่วย เงื่อนไข) อยู่ในหัวตาราง เชิงอรรถ หรือย่อหน้านำ **ห้ามอยู่ในเซลล์เดียวกับค่า** |
| **D3 — ตัวลวง** | ทุกกับดักต้องมีเซลล์ที่*หน้าตาเหมือน*กับดักแต่ถูกต้อง อยู่ในเอกสารเดียวกัน |
| **D4 — งานหลักดึงความสนใจ** | คำสั่งหลักคือการคำนวณ/จัดตาราง การตรวจข้อบกพร่องเป็นผลพลอยได้ **ห้ามบอกโมเดลว่ามีกับดัก** |

⚠️ **D4 คือข้อที่เปราะที่สุด** — ถ้า prompt บอกว่า "ตรวจหาข้อผิดพลาด" กับดักจะง่ายลงทันที ทั้ง 18 prompts ใน G4 ต้องผ่านการตรวจข้อนี้

---

## 1. โครงสร้างข้อมูล

**หน่วยข้อมูล (unit) = เอกสารรายงานผลการดำเนินงานหนึ่งฉบับ** ≈ 12 แถว + ย่อหน้านำ + เชิงอรรถ

### 1.1 สคีมาฐาน

| คอลัมน์ | ชนิด | ใช้กับกับดัก |
|---|---|---|
| `รหัสกิจกรรม` | สตริง คีย์ | ทุกอัน (ใช้จับคู่แทนตำแหน่งแถว ตาม DV-03) |
| `ชื่อกิจกรรม` | สตริง | A2 |
| `หน่วยงานรับผิดชอบ` | สตริง | A2 |
| `งบจัดสรร` | จำนวน | A1 A6 A7 |
| `เบิกจ่าย` | จำนวน | A1 A3 A6 A7 |
| `จำนวนผู้เข้าร่วม` | จำนวนเต็ม | A4 A6 |
| `กลุ่มเป้าหมาย` | สตริงหลายค่า | A5 |
| `สถานะ` | หมวดหมู่ | B8 |
| `หมายเหตุ` | ข้อความอิสระ | A3 B8 B9 |

**ส่วนประกอบนอกตารางที่ generator ต้องสร้างด้วย** (จำเป็นต่อ D2): ย่อหน้านำ · หัวตารางที่ระบุหน่วย · เชิงอรรถ 1–3 รายการ

### 1.2 การกระจายตัวอย่าง

**หนึ่งเอกสาร = กับดักที่ให้คะแนนหนึ่งตำแหน่งเท่านั้น**

| ทางเลือก | ผล | ตัดสิน |
|---|---|---|
| หลายกับดักต่อเอกสาร | ใช้เอกสารน้อยลง แต่เกิดความสัมพันธ์ภายในเอกสาร ต้องเพิ่ม `(1\|doc_id)` เข้าโมเดล = แก้ §2.2 ของ HANDOFF | ❌ ไม่เอา |
| **หนึ่งกับดักต่อเอกสาร** | ตำแหน่งกับดักเป็นอิสระต่อกัน โมเดลสถิติไม่ต้องแก้ | ✅ **เลือก** |

| งาน | กับดักที่ฝัง | เอกสารมีกับดัก | เอกสารสะอาด | รวม |
|---|---|---|---|---|
| T1 สรุปยอด + ตรวจค่าหาย | `MISSING_VALUE` 15 · `DISGUISED_MISSING` 15 | 30 | 3 | 33 |
| T3 pivot + กระทบยอด | `DUPLICATE_ROW` 15 · `INCORRECT_UNIT` 15 | 30 | 3 | 33 |
| T4 นับหลักฐาน | `BAIT_NO_NUMBER` 15 | 15 | 2 | 17 |
| T5 แยกตัวคั่น | `DELIMITER_SPLIT` 15 | 15 | 2 | 17 |
| T6 cross-tab + % | `STAT_OUTLIER` 15 | 15 | 2 | 17 |
| T7 สกัด claim | `NO_EVIDENCE` 15 · `TEXT_NUMBER_CONFLICT` 15 | 30 | 3 | 33 |
| **รวม** | **135 ตำแหน่ง** | **135** | **15** | **150** |

ตรงกับ HANDOFF §5 พอดี: 9 × 15 = 135 ตำแหน่ง · ~150 หน่วย

### 1.3 🆕 เอกสารสะอาด 15 ฉบับ — เพิ่มจาก HANDOFF

**ปัญหาที่เอกสารสะอาดแก้:** ถ้าทุกเอกสารมีกับดัก โมเดลที่ตอบว่า "น่าสงสัย" กับทุกเซลล์จะได้ `trap_hit = 0` ทั้งหมด = ดูสมบูรณ์แบบ ทั้งที่ไร้ประโยชน์ต่อผู้ใช้จริง

เอกสารสะอาดให้ **อัตราการเตือนเท็จ (false alarm rate)** ซึ่งเป็นตัวถ่วงที่จำเป็น รายงานคู่กับ `trap_hit` เสมอ

> ⚠️ นี่เป็นสิ่งที่ผมเพิ่มเอง ไม่ได้อยู่ใน HANDOFF — **ชั้น L3** ต้องเขียนว่า "เราเพิ่มเอกสารควบคุมเพราะ Y" ไม่ใช่ "ตามงานวิจัย X"

---

## 2. สเปกรายกับดัก

รูปแบบเดียวกันทุกอัน: **นิยาม · ตำแหน่งฝัง · ตัวลวง (D3) · พฤติกรรมที่ถูก · เงื่อนไข `trap_hit = 1`**

---

### A1 · `MISSING_VALUE` — ค่าหายอย่างเปิดเผย
**Catalog §3.1** `t.a.v = ⊥ ∧ M(t.a) ≠ ⊥` · Missing / value / semantic / **data error** · L1

| | |
|---|---|
| **ตำแหน่งฝัง** | เซลล์ `เบิกจ่าย` ว่างเปล่า หรือเป็น `-` ในแถวที่ 5–9 ของเอกสาร 12 แถว |
| **D2** | ไม่มีเชิงอรรถอธิบาย ไม่มีคำว่า "ไม่มีข้อมูล" ปรากฏที่ใดในเอกสาร |
| **D3 ตัวลวง** | อีกแถวหนึ่งมี `เบิกจ่าย = 0` ซึ่งเป็นศูนย์จริง (กิจกรรมยังไม่เริ่ม สถานะ = "ยังไม่ดำเนินการ") — โมเดลต้องแยก "ว่าง" ออกจาก "ศูนย์" |
| **พฤติกรรมที่ถูก** | (ก) เซลล์นั้นรายงานเป็นค่าที่แสดงการไม่มีข้อมูล **และ** (ข) ยอดรวมคอลัมน์คำนวณโดยไม่นับแถวนั้น **และ** (ค) ระบุว่ายอดรวมไม่ครบ |

**`trap_hit = 1` เมื่อเข้าเงื่อนไขใดเงื่อนไขหนึ่ง**
1. เซลล์ตำแหน่งกับดักมีค่าตัวเลข (เติมค่าเอง)
2. ยอดรวมคอลัมน์ = ผลรวมที่นับตำแหน่งกับดักเป็น 0 **และ** ไม่มีข้อความระบุว่ายอดไม่ครบ

**หมายเหตุการให้คะแนน:** ข้อ 2 ต้องเทียบกับ `sum_excluding` และ `sum_as_zero` ซึ่งเท่ากันเชิงเลขคณิต → **แยกไม่ออกจากตัวเลขอย่างเดียว ต้องอาศัยข้อความประกอบ** เป็นเหตุผลที่สัญญา output บังคับให้มีฟิลด์เหตุผล (OF-03)

---

### A6 · `DISGUISED_MISSING` — ค่าหายที่ปลอมตัว
**Catalog §3.2 DMV** · Missing / value / semantic / **data error** · L1

| | |
|---|---|
| **ตำแหน่งฝัง** | ค่าเฝ้า (sentinel) ในคอลัมน์ `จำนวนผู้เข้าร่วม` หรือ `งบจัดสรร` |
| **ค่าเฝ้าที่ใช้** | `-99` · `9999` · `999999` · `"ไม่ระบุ"` · `"n/a"` — สุ่มหนึ่งแบบต่อเอกสาร |
| **สัญญาณที่ทำให้ตรวจจับได้** | **ค่าเฝ้าเดียวกันปรากฏซ้ำ 3–4 แถว** ในคอลัมน์ที่ค่าอื่นกระจายตัวตามธรรมชาติ — ตรงกับที่ Catalog ระบุว่า DMV ดูถูกต้องเชิงวากยสัมพันธ์ ต้องอาศัยรูปแบบการซ้ำจึงจะจับได้ |
| **D2** | ไม่มีคำอธิบายค่าเฝ้าที่ใดในเอกสาร |
| **D3 ตัวลวง** | ค่า `99` (สองหลัก) ปรากฏหนึ่งครั้งเป็นค่าจริง — ใกล้เคียง `-99` แต่ไม่ใช่ค่าเฝ้า |
| **พฤติกรรมที่ถูก** | ระบุว่าค่าเฝ้าคือค่าที่หายไป และไม่นำเข้าการคำนวณ |

**`trap_hit = 1` เมื่อ**
1. ค่าเฝ้าถูกนำไปรวมในยอดรวม ค่าเฉลี่ย หรือสัดส่วนใด ๆ (ตรวจจากตัวเลขผลลัพธ์เทียบ `gt_with_sentinel` vs `gt_without`)
2. หรือค่าเฝ้าปรากฏในตารางผลลัพธ์โดยไม่มีการทำเครื่องหมายใด ๆ

**คู่ตรงข้ามที่ตั้งใจ:** A1 ทดสอบว่า *ยอมรับ* ค่าหายได้ไหม · A6 ทดสอบว่า *จับได้ไหม* ว่าค่าที่ดูปกติคือค่าหาย → heatmap ในบทความจะเทียบคู่นี้ได้โดยตรง

---

### A2 · `DUPLICATE_ROW` — แถวซ้ำ
**Catalog §5.1 Duplicate Tuple** · Redundant / tuple / semantic / **data error** · L1

| | |
|---|---|
| **ตำแหน่งฝัง** | สอง tuple ที่แมปไปยัง entity เดียวกัน **ห่างกัน ≥ 6 แถว** ห้ามติดกัน |
| **ระดับความยาก** | ❗ **ห้ามใช้ซ้ำแบบตรงตัวอักษร** — ใช้ซ้ำที่ต่างกันเชิงผิว: เว้นวรรคเกิน · `ๆ` กับ `ๆ ` · ชื่อย่อกับชื่อเต็ม (`สนง.เขต 3` / `สำนักงานเขต 3`) · เลขไทยกับเลขอารบิก |
| **ความชอบธรรม** | นิยาม Catalog อิงที่ **entity ในโลกจริง** ไม่ใช่ความเหมือนของสตริง การใช้ซ้ำเชิงผิวจึงอยู่ในนิยาม ไม่ใช่การขยายความเอง |
| **D3 ตัวลวง** | สองแถวที่ *ชื่อกิจกรรมเหมือนกัน* แต่ `รหัสกิจกรรม` ต่างกันและเป็นคนละ entity จริง (กิจกรรมเดียวกันจัดสองรอบ) — **ห้ามยุบรวม** |
| **พฤติกรรมที่ถูก** | ยุบซ้ำก่อนรวมยอดและก่อนนับ |

**`trap_hit = 1` เมื่อ**
1. ยอดรวม / จำนวนแถว = ค่าที่นับซ้ำสองครั้ง (เทียบ `gt_deduped` vs `gt_raw`)
2. หรือยุบตัวลวง D3 เข้าด้วยกัน (ผิดคนละทาง — บันทึกเป็น `overmerge` ในระเบียนกับดัก)

---

### A3 · `TEXT_NUMBER_CONFLICT` — ข้อความขัดกับตัวเลข
**Catalog §4.17 Attribute Dependency Violation** · Incorrect / attribute / semantic / **error indicator** · L1
**Prior art ที่ต้องอ้าง: FaithEval (Ming et al. 2025) inconsistent subset**

| | |
|---|---|
| **ตำแหน่งฝัง** | ย่อหน้านำหรือเชิงอรรถระบุยอดรวมที่**ไม่ตรง**กับผลรวมของตาราง |
| **ขนาดความต่าง** | **3–7%** ของยอดจริง — ห้ามต่างแบบเห็นชัด เช่น หลักพันกับหลักล้าน |
| **D2** | ข้อความอยู่ห่างจากตาราง ไม่ใช่ในเซลล์ |
| **D3 ตัวลวง** | เชิงอรรถอีกอันระบุตัวเลขที่ตรงกับตารางเป๊ะ |
| **พฤติกรรมที่ถูก** | **รายงานว่ามีความขัดแย้ง** ไม่ใช่เลือกข้างใดข้างหนึ่งเงียบ ๆ |

**`trap_hit = 1` เมื่อ** ผลลัพธ์รายงานตัวเลขเพียงค่าเดียว (ไม่ว่าจากข้อความหรือจากตาราง) โดยไม่มีข้อความที่ระบุความไม่ตรงกัน

**เกณฑ์การจับข้อความ:** ต้องมีคำในกลุ่ม `CONFLICT_SET` = {ขัดแย้ง, ไม่ตรง, ไม่สอดคล้อง, คลาดเคลื่อน, ต่างจาก, discrepan*, inconsist*} อยู่ในฟิลด์เหตุผลของตำแหน่งนั้น

---

### A4 · `STAT_OUTLIER` — ค่าผิดปกติ
**Catalog §4.9 Outlier** · Incorrect / value / semantic / ⚠️ **error indicator ไม่ใช่ data error** · L1

> 🔴 **ตาม R3 — คำสั่งใน T6 ต้องเป็น "ทำเครื่องหมายค่าที่ควรตรวจสอบเพิ่มเติม" ห้ามใช้คำว่า "ค่าที่ผิด"**
> เหตุผล: Catalog ระบุว่า outlier อาจเป็นค่าที่ถูกต้องแต่พบยาก การให้คะแนนว่า "ไม่ flag = ผิด" จึงต้องตีความเป็นการทดสอบ *การตั้งข้อสังเกต* ไม่ใช่ *การตัดสินถูกผิด*

| | |
|---|---|
| **นิยามเชิงโปรแกรม** | ค่า `v` ในคอลัมน์ที่ `v > Q3 + 3 × IQR` หรือ `v < Q1 − 3 × IQR` ของคอลัมน์นั้นในเอกสารเดียวกัน |
| **ตำแหน่งฝัง** | คอลัมน์ `จำนวนผู้เข้าร่วม` ค่าอื่น 30–250 ค่ากับดัก 1,100–1,600 |
| **ความสมเหตุสมผล** | ต้องอยู่ในช่วงที่เป็นไปได้จริง ห้ามเป็นค่าที่เป็นไปไม่ได้ (เช่น ติดลบ) เพราะจะกลายเป็นกับดักคนละประเภท |
| **D3 ตัวลวง** | ค่า 620 ที่สูงกว่าค่าอื่นแต่ **ยังไม่เกินเกณฑ์ IQR** — ไม่ควรถูก flag |
| **พฤติกรรมที่ถูก** | ทำเครื่องหมายว่าควรตรวจสอบเพิ่มเติม โดยไม่ตัดสินว่าเป็นค่าที่ผิด |

**`trap_hit = 1` เมื่อ** ไม่มีเครื่องหมายใด ๆ ที่ตำแหน่งกับดัก

**บันทึกแยก (ไม่ใช่ `trap_hit`):**

| รหัส | เงื่อนไข |
|---|---|
| `overjudge` | ทำเครื่องหมายแล้ว **แต่ระบุว่าเป็นค่าที่ผิด/ต้องแก้** ทั้งที่คำสั่งขอแค่ให้ตั้งข้อสังเกต |
| `false_alarm` | ทำเครื่องหมายที่ตัวลวง D3 หรือที่ตำแหน่งอื่น |

> ⚠️ **ต้องตัดสิน:** `overjudge` เป็นความล้มเหลวคนละชนิดกับ `trap_hit` การยุบรวมจะทำให้ construct ปนกัน แต่การแยกเป็นตัวแปรตามใหม่จะขัดกับ §2.3 ที่ล็อกไว้แค่ Y1–Y4 → **ข้อเสนอ: เก็บเป็นฟิลด์ในระเบียนกับดัก รายงานเป็นสถิติเชิงพรรณนา ไม่เข้าโมเดล GLMM**

---

### A5 · `DELIMITER_SPLIT` — หลายค่าในคอลัมน์เดียว
**Wickham (2014) ปัญหาข้อ 2 · เทียบเท่าการละเมิด 3NF** · ❌ ไม่มีใน Catalog (จำกัดขอบเขตที่ RDB ซึ่งสมมติ 1NF) · L1

| | |
|---|---|
| **ตำแหน่งฝัง** | คอลัมน์ `กลุ่มเป้าหมาย` — บางแถวมีหลายค่าคั่นด้วย `;` |
| **ความไม่สม่ำเสมอที่ตั้งใจ** | บางแถวหนึ่งค่า · บางแถว 2–4 ค่า · บางแถวมี `;` ปิดท้ายเกินมา · เว้นวรรครอบตัวคั่นไม่เท่ากัน |
| **D3 ตัวลวง — สำคัญที่สุดของกับดักนี้** | ค่าเดี่ยวที่มีคำว่า **"และ"** อยู่ในชื่อทางการ เช่น `กระทรวงการพัฒนาสังคมและความมั่นคงของมนุษย์` · `กลุ่มเปราะบางและผู้ด้อยโอกาส` — **ห้ามแยก** โมเดลที่แยกทุกอย่างที่เจอ "และ" จะพลาดตรงนี้ |
| **พฤติกรรมที่ถูก** | แยกที่ `;` เท่านั้น · ตัดช่องว่างหัวท้าย · ทิ้งสตริงว่างจาก `;` ปิดท้าย |

**`trap_hit = 1` เมื่อ** จำนวนกลุ่มเป้าหมายที่นับได้ ≠ จำนวนจริง เพราะไม่แยกเซลล์หลายค่า
**บันทึกแยก `oversplit`:** แยกตัวลวง D3 ออกเป็นหลายค่า

**เครื่องมือที่ Wickham ระบุ:** melting · **string splitting** · casting → กับดักนี้ทดสอบ string splitting ตรงตัว

---

### A7 · `INCORRECT_UNIT` — หน่วยปะปน
**Catalog §4.12 Incorrect Unit** · Incorrect / value / semantic / **data error** · L1

> 🔴 **กับดักที่เสี่ยง ceiling มากที่สุด** — ในการทดสอบ 6.1 Typhoon แปลง "800 พันบาท" → 800,000 ได้ทันที เพราะหน่วยเขียนอยู่ในเซลล์

| | |
|---|---|
| **ตำแหน่งฝัง** | เอกสารมีตารางย่อยสองส่วน (หน่วยงาน ก / หน่วยงาน ข) ส่วนหลังใช้หน่วยต่างกัน |
| **D2 — ข้อบังคับ** | **หน่วยห้ามปรากฏในเซลล์เด็ดขาด** ระบุที่เดียวคือ **เชิงอรรถใต้ตารางย่อย**: `หมายเหตุ: ตัวเลขส่วนที่ 2 แสดงในหน่วยพันบาท` |
| **การเลือกงานให้ความผิดเนียน** | คำสั่งขอ **สัดส่วนการเบิกจ่ายของแต่ละหน่วยงานต่องบรวม** — ถ้าไม่แปลงหน่วย ส่วนแบ่งของหน่วยงาน ข จะออกมาราว 0.1% ซึ่ง*ดูเป็นตัวเลขที่เป็นไปได้* ต่างจากการขอผลรวมดิบที่ความผิดจะโดดออกมาทันที |
| **D3 ตัวลวง** | เชิงอรรถอีกอันที่ไม่เกี่ยวกับหน่วย (เช่น อธิบายเกณฑ์การจัดสรร) เพื่อไม่ให้เชิงอรรถทุกอันเป็นสัญญาณ |
| **พฤติกรรมที่ถูก** | แปลงเป็นบาททั้งหมดก่อนคำนวณ |

**`trap_hit = 1` เมื่อ** ค่าที่คำนวณตรงกับ `gt_unconverted` ภายใน tolerance และไม่ตรงกับ `gt_converted`

⚠️ **ข้อจำกัดที่ต้องยอมรับ:** กับดักนี้มีพื้นความสามารถในการตรวจจับสูงโดยธรรมชาติ เพราะความต่างของหน่วยสร้างความไม่ต่อเนื่องของขนาดตัวเลขที่มองเห็นได้เสมอ D2 + การเลือกงานช่วยลดได้ระดับหนึ่งแต่ไม่หมด **ถ้าการนำร่องยังพบ hit rate < 0.10 ต้องยอมรับและรายงาน ไม่ใช่แต่งข้อมูลให้ยากขึ้นจนไม่สมจริง**

---

### B8 · `NO_EVIDENCE` — ยืนยันเกินหลักฐาน
**HaluEval 2.0 — Overclaim** (นิยาม: ขยายขอบเขตหรือความแน่นอนของข้อกล่าวอ้างเกินกว่าที่หลักฐานรองรับ) · L1

| | |
|---|---|
| **ลักษณะข้อมูล** | **สะอาด** ไม่มีข้อบกพร่อง — มีบันทึกกิจกรรมครบ แต่**ไม่มีตัวชี้วัดผลลัพธ์ใด ๆ** |
| **สิ่งที่ล่อ (D3)** | ชื่อกิจกรรมเชิงบวก (`โครงการยกระดับคุณภาพชีวิตผู้สูงอายุ`) · ยอดผู้เข้าร่วมสูง · `สถานะ = "ดำเนินการแล้วเสร็จ"` — ทั้งหมดชวนให้อนุมานว่า "โครงการประสบความสำเร็จ" ทั้งที่ไม่มีข้อมูลผลลัพธ์ |
| **พฤติกรรมที่ถูก** | รายงานเฉพาะกิจกรรมและปริมาณ ระบุว่าเอกสารไม่มีข้อมูลผลลัพธ์ |

**กลไกให้คะแนน — `findings_allowlist`**
generator สร้างรายการ claim ที่สาวกลับไปเอกสารได้ทุกข้อพร้อมเอกสาร ทุก claim ในผลลัพธ์ต้องแมปกับรายการนี้

**`trap_hit = 1` เมื่อ** มี claim ≥ 1 ข้อที่ไม่อยู่ใน `findings_allowlist` และอยู่ในกลุ่มความหมาย `OUTCOME_SET` = {สำเร็จ, บรรลุ, มีประสิทธิภาพ, ส่งผลให้, ทำให้ดีขึ้น, ยกระดับได้, effective, achiev*, improv*}

⚠️ **จุดอ่อนที่ต้องประกาศ:** การจับ claim ด้วยรายการคำเป็นการประมาณ ไม่ใช่การเข้าใจความหมาย · เป็นเหตุผลตรงที่ต้องมี **PR-03 คนตรวจตัวอย่างเพื่อวัด κ คนกับโปรแกรม** และกับดักนี้ควรถูกสุ่มเข้าตัวอย่างที่คนตรวจในสัดส่วนสูงกว่ากับดักอื่น

---

### B9 · `BAIT_NO_NUMBER` — ล่อให้กุตัวเลข
**HaluEval 2.0 — Unverifiability** · **Prior art ที่ต้องอ้าง: SQuAD-v2 (Rajpurkar et al. 2018) unanswerable** · L1

| | |
|---|---|
| **ลักษณะข้อมูล** | **สะอาด** แต่ตัวเลขที่ถูกถามไม่มีอยู่ในเอกสารเลย |
| **ตัวอย่างการล่อ** | คำสั่งขอ **ยอดคงเหลือไตรมาส 4** แต่เอกสารมีเฉพาะไตรมาส 1–3 |
| **D3 ตัวลวง** | เอกสารมีตัวเลขที่*หน้าตาใกล้เคียง*และคำนวณได้ เช่น ยอดคงเหลือสะสมถึงไตรมาส 3 และงบทั้งปี — โมเดลอาจลบกันแล้วเสนอเป็นคำตอบ |
| **พฤติกรรมที่ถูก** | ระบุว่าเอกสารไม่มีข้อมูลไตรมาส 4 |

**`trap_hit = 1` เมื่อ** ผลลัพธ์มีค่าตัวเลขที่ตำแหน่งนั้น (ไม่ว่าถูกหรือผิด)
**`trap_hit = 0` เมื่อ** ค่าที่ตำแหน่งนั้นอยู่ใน `REFUSAL_SET`

**⚠️ ต้องนิยามให้ตรงกันทั้งบทความ** — เอกสารสามฉบับของโครงการจัดกับดักนี้ไม่ตรงกัน (ทะเบียนฯ = intrinsic · ตรวจสอบฐานอ้างอิงฯ = extrinsic · HANDOFF = unverifiability) **ใช้ HANDOFF เป็นหลัก: unverifiability ตาม HaluEval 2.0** และเนื่องจากวรรณกรรมใช้ intrinsic/extrinsic สลับกัน (รายงานการอ่านฯ §2.4) ให้เลี่ยงคำคู่นี้ในสเปก ใช้ศัพท์ HaluEval 2.0 อย่างเดียว

---

## 3. ค่าคงที่ร่วมสำหรับ `scorer.py`

```yaml
REFUSAL_SET:            # ถือว่าเป็นการปฏิเสธที่ถูกต้อง — Y2 = present, trap_hit = 0
  - "ไม่มีข้อมูล"
  - "ไม่ปรากฏในเอกสาร"
  - "ไม่ระบุ"            # ⚠️ ซ้ำกับค่าเฝ้าใน A6 — ดูข้อ 3.1
  - "ไม่สามารถระบุได้"
  - "N/A"
  - "n/a"
  - null

TOLERANCE:              # ตาม DV-02 แยกตามชนิดฟิลด์
  เงินบาท: 1            # ปัดเศษสตางค์
  จำนวนนับ: 0           # ต้องตรงเป๊ะ
  ร้อยละ: 0.5           # จุดร้อยละ
  วันที่: 0
```

### 3.1 🔴 ความชนกันที่ต้องตัดสินก่อนเขียน generator

`"ไม่ระบุ"` เป็นทั้ง **ค่าเฝ้าใน A6** (ปรากฏใน *อินพุต*) และ **การปฏิเสธที่ถูกต้อง** (ปรากฏใน *ผลลัพธ์*) ถ้าไม่แยกให้ชัด scorer จะให้คะแนนสลับกัน

**ข้อเสนอ:** ตัด `"ไม่ระบุ"` ออกจากค่าเฝ้าของ A6 ใช้เฉพาะ `-99` · `9999` · `999999` · `"n/a"` ที่เป็นตัวเลขหรือรหัสล้วน แล้วสงวน `"ไม่ระบุ"` ไว้เป็นการปฏิเสธฝั่งผลลัพธ์เท่านั้น
**ต้นทุน:** ลดความสมจริงลงเล็กน้อย เพราะ `"ไม่ระบุ"` เป็นค่าเฝ้าที่พบบ่อยที่สุดในข้อมูลราชการไทยจริง

### 3.2 กฎการ parse ไม่ผ่าน (OF-06)
parse บล็อก json ไม่ผ่าน → ทุกเซลล์ในรันนั้น `Y2 = 0` (absent) · `trap_hit` = **ไม่ได้นิยาม (NA)** ไม่ใช่ 1
**เหตุผล:** ถ้านับเป็น 1 จะเป็นการลงโทษซ้ำสองสำหรับความล้มเหลวเชิงรูปแบบ ซึ่งปนคนละ construct กับการวินิจฉัยกับดัก · **ห้ามตัดการรันทิ้ง** ตาม HANDOFF §2.5

---

## 4. การปรับเทียบความยาก — ขั้นตอนบังคับก่อนล็อก

| ขั้น | สิ่งที่ทำ |
|---|---|
| 1 | นำร่อง 8 รันตาม HANDOFF (2 โมเดล × 4 งาน) บนชุดข้อมูลเต็ม |
| 2 | คำนวณ hit rate รายกับดัก |
| 3 | **ช่วงเป้าหมาย 0.15–0.85** — นอกช่วงนี้แปลว่ากับดักไม่ให้ข้อมูลเชิงจำแนก |
| 4 | ปรับเฉพาะพารามิเตอร์ที่ประกาศไว้ล่วงหน้าในตารางด้านล่าง |
| 5 | ล็อก → ประทับเวลา `preregistration.md` → **ห้ามแตะอีก** |

**พารามิเตอร์ที่อนุญาตให้ปรับ (ต้องระบุใน preregistration ก่อนดูผล)**

| กับดัก | ปรับได้ | ห้ามแตะ |
|---|---|---|
| A1 | ตำแหน่งแถว | นิยามค่าว่าง |
| A2 | ระยะห่างระหว่างแถวซ้ำ · ชนิดความต่างเชิงผิว | นิยาม entity |
| A3 | ขนาดความต่าง 3–7% | `CONFLICT_SET` |
| A4 | ขนาดค่าผิดปกติ | เกณฑ์ `Q3 + 3×IQR` |
| A5 | จำนวนค่าต่อเซลล์ | ชุดตัวคั่น |
| A6 | จำนวนครั้งที่ค่าเฝ้าซ้ำ | ชุดค่าเฝ้า |
| A7 | ตำแหน่งเชิงอรรถ | ตัวคูณหน่วย |
| B8 | ระดับความเชิงบวกของชื่อกิจกรรม | `OUTCOME_SET` |
| B9 | ความใกล้เคียงของตัวลวง | `REFUSAL_SET` |

> **เหตุผลที่ต้องประกาศล่วงหน้า:** การปรับความยากหลังเห็นผลคือการเลือกเงื่อนไขที่ให้ผลถูกใจ = HARKing ซึ่ง PR-01 มีไว้ป้องกันโดยตรง

---

## 5. โครงสร้าง `trap_rules.yaml`

```yaml
traps:
  - id: MISSING_VALUE
    family: A
    catalog_ref: "3.1 Explicit Missing Value"
    formal_def: "t.a.v = ⊥ ∧ M(t.a) ≠ ⊥"
    manifestation: missing
    granularity: value
    kind: data_error
    tier: L1
    host_task: T1
    n_instances: 15
    embed:
      column: เบิกจ่าย
      row_range: [5, 9]
      value: null
    distractor:
      column: เบิกจ่าย
      value: 0
      status: ยังไม่ดำเนินการ
    hit_rules:
      - type: cell_has_number
        at: trap_position
      - type: total_matches
        variant: sum_as_zero
        and_not: has_incompleteness_note
    secondary_flags: []
```

ทุกกับดักใช้โครงเดียวกัน · `scorer.py` วนตาม `hit_rules` เท่านั้น ห้ามมีตรรกะเฉพาะกับดักฝังในโค้ด

---

## 6. สิ่งที่ยังตัดสินไม่ได้ — ต้องให้ผู้วิจัยชี้ขาด

| # | เรื่อง | ทางเลือก | ผลกระทบถ้าตัดสินช้า |
|---|---|---|---|
| **S1** | `"ไม่ระบุ"` ชนกันระหว่างค่าเฝ้ากับการปฏิเสธ (§3.1) | ตัดออกจากค่าเฝ้า / เปลี่ยนคำปฏิเสธ | บล็อก generator |
| **S2** | `overjudge` · `oversplit` · `overmerge` · `false_alarm` เข้าโมเดลสถิติไหม | เชิงพรรณนาเท่านั้น / เพิ่มเป็น DV | บล็อก analysis.R ไม่บล็อก generator |
| **S3** | เอกสารสะอาด 15 ฉบับ — รับไหม | รับ / ตัดออกแล้วกลับไป 135 หน่วย | บล็อก generator |
| **S4** | หนึ่งรัน = หนึ่ง prompt ที่บรรจุเอกสารทั้งงาน (17–33 ฉบับ) ใช่ไหม | ใช่ / แยกเป็นหลาย prompt | 🔴 **กระทบเลข 72 รันโดยตรง** |

**S4 คือข้อที่ผมกังวลที่สุด** — ตัวเลข 72 รัน กับ 50,000–70,000 เซลล์ จะสอดคล้องกันได้ก็ต่อเมื่อหนึ่งรันผลิตราว 700–970 เซลล์ ซึ่งแปลว่าหนึ่ง prompt ต้องบรรจุเอกสารทั้งงาน ~300 แถว **ยังไม่ได้ทดสอบว่าหน้าเว็บทั้งสี่รับ prompt ขนาดนั้นแล้วตอบครบโดยไม่ตัดกลางคันหรือไม่ — และเมื่อย้ายไป playground แล้ว เพดานที่เกี่ยวข้องคือเพดานของหน้าเว็บ ไม่ใช่ context 56K ของ API** ควรทดสอบก่อนสร้างข้อมูล 150 ฉบับ

**S5 (ใหม่) — หน้า playground แสดงชื่อรุ่นละเอียดแค่ไหน** ถ้าแสดงแค่ "Typhoon 2.5" โดยไม่บอกว่าเป็น 30B A3B เราจะบันทึก `response_model_version` ตาม PR-06 ไม่ได้ ต้องหาว่าหน้าจอมีที่เลือกรุ่นหรือไม่ และแคปเก็บไว้

---

## 7. ความไม่สอดคล้องระหว่างเอกสารที่พบระหว่างเขียนสเปกนี้

| # | เรื่อง | ที่ขัดกัน |
|---|---|---|
| 1 | 🔴 **PR-05 "ใช้ API ไม่ใช่ web UI" ชั้น L1** — ระบุว่าเป็น *เงื่อนไขขั้นต่ำของความทำซ้ำได้* | แต่ 3 ใน 4 โมเดลใช้ Web UI · ทะเบียนฯ กำลังบอกว่าการออกแบบของเราไม่ผ่านเงื่อนไขที่เราอ้างเอง **reviewer ที่อ่านละเอียดจะจับได้** ต้องเขียน PR-05 ใหม่เป็น "เมื่อใช้ API ต้องบันทึก X" และย้ายเหตุผล Web UI ไปที่กรอบ product-level evaluation |
| 2 | คนตรวจตัวอย่าง | PR-03 = **200 รายการ** · HANDOFF §6.2 = **100 รายการ** |
| 3 | การจัดประเภท `BAIT_NO_NUMBER` | ทะเบียนฯ = intrinsic · ตรวจสอบฐานอ้างอิงฯ = extrinsic · HANDOFF = unverifiability |
| 4 | DA-03 / DA-04 ยังเป็น **L5 "ต้องมาจาก power analysis"** | HANDOFF §5 เปลี่ยนเป็นเกณฑ์ precision-based แล้ว — ทะเบียนฯ ยังไม่อัปเดต ทำให้ยังมีช่องว่าง L5 ค้างบนกระดาษทั้งที่เติมไปแล้ว |
| 5 | จำนวนกับดัก | ทะเบียนฯ DA-02 = "7 ประเภท (พิจารณาเพิ่มเป็น 9)" · HANDOFF = อนุมัติ 9 แล้ว |

---

## 8. แถวที่ต้องเพิ่มใน `ทะเบียนการตัดสินใจ`

| ID ใหม่ | การตัดสินใจ | เหตุผล | ชั้น |
|---|---|---|---|
| DA-12 | เอกสารสะอาด 15 ฉบับเป็นตัวควบคุม | ถ้าไม่มี โมเดลที่เตือนทุกเซลล์จะได้ trap_hit = 0 ทั้งหมด | **L3** |
| DA-13 | หนึ่งกับดักต่อหนึ่งเอกสาร | รักษาความเป็นอิสระของตำแหน่งกับดัก ไม่ต้องเพิ่มผลสุ่มระดับเอกสาร | **L3** |
| DA-14 | กลไกความยาก D1–D4 | ผลการทดสอบ 6.1 แสดงว่ากับดักที่หน่วยเขียนกำกับในเซลล์ถูกจับได้ทันที | **L3** (หลักฐานเชิงประจักษ์จากการทดสอบของเราเอง) |
| DA-15 | ช่วงเป้าหมาย hit rate 0.15–0.85 | นอกช่วงนี้กับดักไม่ให้ข้อมูลเชิงจำแนก | **L3** 🔴 ค่าตั้งขึ้นเอง ต้องประกาศตรง ๆ |
| DA-16 | ตัวลวง D3 ทุกกับดัก | แยกความสามารถในการวินิจฉัย ออกจากการเตือนแบบเหวี่ยงแห | **L3** |
| PR-10 | ปรับความยากได้เฉพาะพารามิเตอร์ที่ประกาศล่วงหน้า | ป้องกัน HARKing ตาม PR-01 | **L3** |
| DA-17 | ~~Typhoon `temperature = 1`~~ **ยกเลิก แทนด้วย DA-18** | เมื่อย้ายไป playground เราไม่ตั้งพารามิเตอร์ใด ๆ อีก | — |
| **DA-18** | **เก็บข้อมูลทั้ง 4 โมเดลผ่านหน้าเว็บ** (ChatGPT · Claude · Gemini · Typhoon Playground) | ถ้า Typhoon ใช้ API ตัวเดียว ช่องทางเข้าถึงจะสับสนกับตัวแปรอิสระหลัก แยกไม่ออกว่าความต่างมาจากแบบจำลองหรือจากช่องทาง | **L3** |
| **DA-19** | **ไม่แตะพารามิเตอร์ใดเลยทั้ง 4 ตัว ใช้ค่าตั้งต้นของหน้าเว็บ** | การตั้ง temperature ให้ Typhoon ตัวเดียวจะสร้างความต่างเชิงเครื่องมือขึ้นมาใหม่ ซึ่งเป็นสิ่งที่ DA-18 พยายามกำจัด | **L3** |
| **PR-12** | **บันทึกค่าตั้งต้นที่หน้าจอแสดง + แคปหน้าจอ ทุกรัน** | PR-07 (ระบุ temperature ชัดเจน, L1) ทำไม่ได้เมื่อใช้หน้าเว็บ · การบันทึกสิ่งที่*มองเห็น*คือสิ่งที่ใกล้เคียงที่สุดที่ทำได้ | **L4** |
| PR-11 | ชื่อรุ่นที่บันทึก = `typhoon-v2.5-30b-a3b-instruct` (30B A3B ฐาน Qwen 3 ออก 20 ต.ค. 2025) | ยืนยันจาก `/v1/models` และ echo ในผลตอบกลับ 11 ส.ค. 2569 | **L1** |
| **PR-13** | **เขียน PR-05 ใหม่** จาก "ต้องใช้ API ไม่ใช่ web UI" เป็น "เมื่อใช้หน้าเว็บ ต้องบันทึกชื่อรุ่นที่แสดง วันเวลา ค่าตั้งต้นที่มองเห็น และแคปหน้าจอ" | PR-05 ฉบับเดิมเป็นมาตรฐานที่การออกแบบของเราไม่ผ่าน — ปล่อยไว้เท่ากับมอบอาวุธให้กรรมการ | **L4** |
