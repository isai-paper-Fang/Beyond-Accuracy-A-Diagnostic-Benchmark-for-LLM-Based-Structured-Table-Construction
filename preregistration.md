# Preregistration
## การประเมินเชิงวินิจฉัยความสามารถของแบบจำลองภาษาขนาดใหญ่ในการสร้างตารางข้อมูลมีโครงสร้างจากรายงานผลการดำเนินงานภาษาไทย

**ฉบับร่างเพื่อล็อก · 11 ส.ค. 2569 · ผู้เขียนปกปิดตามข้อกำหนด double-anonymous**

> 🔴 **เอกสารนี้ยังไม่มีผลจนกว่าจะทำสองอย่าง**
> 1. รัน `python lock_files.py` เพื่อบันทึกลายนิ้วมือไฟล์ทั้งหมด
> 2. ประทับเวลาโดยวิธีที่ตรวจสอบย้อนหลังได้ (§13)
>
> **ห้ามเก็บข้อมูลแม้แต่รันเดียวก่อนสองข้อนี้เสร็จ** รวมถึงรันนำร่อง

---

## 1. คำถามวิจัย

| RQ | คำถาม | ประเภท |
|---|---|---|
| **RQ1** | ผลิตภัณฑ์ผู้ช่วย AI ทั้งสี่ต่างกันหรือไม่ในความถูกต้องระดับเซลล์ | ยืนยันสมมติฐาน |
| **RQ2** | ผลิตภัณฑ์ใดล้มเหลวที่กับดักประเภทใด — **แกนหลักของบทความ** | สำรวจ |
| **RQ3** | ความแปรปรวนจากการรันซ้ำใหญ่เพียงใดเมื่อเทียบกับความต่างระหว่างผลิตภัณฑ์ | สำรวจ |

**สมมติฐานที่ตั้งไว้ล่วงหน้า:** ไม่มี — เราไม่มีฐานทฤษฎีที่จะทำนายว่าผลิตภัณฑ์ใดจะพลาดที่กับดักใด **RQ2 และ RQ3 เป็นการสำรวจโดยประกาศ** ไม่ใช่การทดสอบสมมติฐานที่แต่งขึ้นภายหลัง

---

## 2. สิ่งที่ถูกวัด

**หน่วยวิเคราะห์: เซลล์** ประมาณ 60,000 หน่วยสังเกต

| รหัส | ตัวแปร | หน่วย | นิยามปฏิบัติการ |
|---|---|---|---|
| Y1 | `correct_cell` | เซลล์ | ตรงเฉลยภายใน tolerance ตามชนิดฟิลด์ (`trap_rules.yaml`) |
| Y2 | `present_cell` | เซลล์ | เซลล์ที่เฉลยกำหนดปรากฏในผลลัพธ์ |
| Y3 | `fabricated` | claim | claim ที่ไม่อยู่ใน `findings_allowlist` · รายงานคู่กับ `claims_produced` |
| Y4 | `trap_hit` | รัน × กับดัก × ตัวอย่าง | ตามกฎใน `trap_rules.yaml` |

**ตัวแปรรอง — รายงานเชิงพรรณนาเท่านั้น ไม่เข้าโมเดล GLMM**
`overjudge` · `false_alarm` · `overmerge` · `oversplit` · `claims_produced` · `parse_status`

**ตัดออกแล้วและจะไม่นำกลับมา:** `Y_composite` · verbalized confidence · AUC/DeLong · provenance ต่อเซลล์ · `internal_consistency`

---

## 3. แบบการทดลอง

**4 ผลิตภัณฑ์ × 6 งาน × 3 replicate = 72 รัน** · ครบทุกช่อง ตรวจความสมดุลแล้ว

| | |
|---|---|
| ผลิตภัณฑ์ | ChatGPT · Claude · Gemini · Typhoon (`typhoon-v2.5-30b-a3b-instruct`) |
| ช่องทาง | **หน้าเว็บทั้งสี่** — Typhoon ใช้ `playground.opentyphoon.ai` |
| พารามิเตอร์ | **ไม่แตะเลย** ใช้ค่าตั้งต้นที่หน้าจอแสดง บันทึกสิ่งที่มองเห็น |
| replicate | rep 1/2/3 ใช้ paraphrase 1/2/3 (ยุบ prompt variant เข้า replicate) |
| ลำดับการรัน | สุ่มด้วย seed `20260815` ล็อกไว้ใน `run_log_template.csv` |
| ข้อมูล | 150 เอกสารสังเคราะห์ (135 มีกับดัก + 15 สะอาด) seed `20260815` |

---

## 4. กับดัก 9 ประเภท

รายละเอียดครบใน `trap_spec.md` · กฎการให้คะแนนใน `trap_rules.yaml`

| ตระกูล | กับดัก | ฐานอ้างอิง | งานที่ฝัง |
|---|---|---|---|
| A | `MISSING_VALUE` · `DISGUISED_MISSING` | Catalog §3.1 · §3.2 | T1 |
| A | `DUPLICATE_ROW` · `INCORRECT_UNIT` | Catalog §5.1 · §4.12 | T3 |
| A | `DELIMITER_SPLIT` | Wickham 2014 ปัญหาข้อ 2 = ละเมิด 3NF | T5 |
| A | `STAT_OUTLIER` | Catalog §4.9 — **error indicator ไม่ใช่ data error** | T6 |
| A | `TEXT_NUMBER_CONFLICT` | Catalog §4.17 · prior art FaithEval | T7 |
| B | `NO_EVIDENCE` | HaluEval 2.0 — overclaim | T7 |
| B | `BAIT_NO_NUMBER` | HaluEval 2.0 — unverifiability · prior art SQuAD-v2 | T4 |

**ประเภทละ 15 ตัวอย่าง = 135 ตำแหน่ง**

---

## 5. ขนาดตัวอย่างและเหตุผล

ใช้ **เกณฑ์ความแม่นยำ** แทน power analysis เชิงจำลอง เนื่องจากข้อจำกัดด้านเวลา

ครึ่งความกว้าง CI 95% ที่ p = 0.5 คือ `1.96 × √(0.25/n)`

| ตัวอย่าง/กับดัก | n ต่อ (ผลิตภัณฑ์ × กับดัก) | ครึ่งความกว้าง CI |
|---|---|---|
| 8 | 24 | ±20.0 pp |
| **15** | **45** | **±14.6 pp** ← เลือก |

> **ประโยคสำหรับบทความ:** เรากำหนดขนาดตัวอย่างจากเกณฑ์ความแม่นยำ โดยต้องการให้ครึ่งความกว้างของช่วงเชื่อมั่น 95% ของสัดส่วนรายกับดักไม่เกิน 15 จุดร้อยละ

⚠️ **คำนวณนี้ไม่ได้คิดผลของ clustering** ความแม่นยำจริงจะแย่กว่านี้ ตัวเลข ±14.6 pp จึงเป็น**ขอบล่างของความกว้าง** ไม่ใช่ค่าที่คาดว่าจะได้ ต้องเขียนตรง ๆ ในบทความ

---

## 6. แบบจำลองสถิติ

### M1 — RQ1 (ยืนยันสมมติฐาน)
```
correct_cell ~ product + (1|task) + (1|doc_id) + (1|run_id)
family = binomial
```

### M2 — RQ2 (สำรวจ) — แกนหลัก
```
trap_hit ~ product * trap_type + (1|doc_id) + (1|run_id)
family = binomial
```

### RQ3
องค์ประกอบความแปรปรวนจาก M1 · เทียบ SD ของผลสุ่มระดับ `run_id` กับขนาดผลของ product · Fleiss' κ ข้าม replicate · instability → error odds ratio

### กติกาที่ล็อกไว้

| รายการ | ค่า | ที่มา |
|---|---|---|
| การทดสอบ | LRT ไม่ใช่ Wald | ST-08, L3 |
| α | .05 สองทาง | ธรรมเนียม |
| แก้ค่าหลายการทดสอบ | **Holm กับ RQ1** (คุม FWER) · **BH กับ RQ2** (คุม FDR) | ST-09, L3 — การจับคู่นี้เป็นวิจารณญาณของเรา |
| overdispersion | ตรวจด้วย DHARMa **ทุกโมเดล** จัดการด้วยผลสุ่ม `(1|run_id)` | Warton & Hui 2011, L1 |
| ค่าประมาณที่รายงาน | Estimated Marginal Means + effect size + CI | Miller 2024, L1 |
| ความไม่แน่นอนเพิ่มเติม | cluster bootstrap ที่ระดับ task | ST-11, L3 |
| การแปลงข้อมูล | **ไม่ใช้ arcsine** | Warton & Hui 2011, L1 |

---

## 7. 🔴 ข้อจำกัดเชิงโครงสร้างที่ต้องประกาศก่อน ไม่ใช่หลังเห็นผล

### 7.1 `trap_type` สับสนกับ `task`
กับดักแต่ละประเภทฝังอยู่ในงานเดียวเท่านั้น **ผลหลักของ `trap_type` จึงแยกจากผลของ `task` ไม่ได้**

| การเปรียบเทียบ | ตีความได้ไหม |
|---|---|
| ผลิตภัณฑ์ A เทียบ B **ภายในกับดักเดียวกัน** | ✅ สะอาด เพราะ product ไขว้กับทุกอย่างเต็มรูปแบบ |
| กับดัก X เทียบ Y **ภายในงานเดียวกัน** (เช่น `MISSING_VALUE` เทียบ `DISGUISED_MISSING` ใน T1) | ✅ สะอาด |
| กับดัก X เทียบ Y **ข้ามงาน** | ❌ **สับสนกับงาน ห้ามตีความ** |

> **ผลต่อการเขียนบทความ:** ประโยคแบบ "โมเดลพลาดที่กับดัก X มากกว่ากับดัก Y" เขียนได้เฉพาะเมื่อ X กับ Y อยู่ในงานเดียวกัน คู่ที่เขียนได้มีสามคู่เท่านั้น: (`MISSING_VALUE`, `DISGUISED_MISSING`) · (`DUPLICATE_ROW`, `INCORRECT_UNIT`) · (`NO_EVIDENCE`, `TEXT_NUMBER_CONFLICT`)
> **แกนของ RQ2 คือปฏิสัมพันธ์ product × trap ซึ่งประมาณได้สะอาด** — ไม่กระทบข้อค้นพบหลัก แต่ต้องไม่เผลอเขียนเกิน

### 7.2 ช่องทางเข้าถึง
Typhoon เข้าถึงผ่าน playground สำหรับนักพัฒนา อีกสามตัวเป็นผลิตภัณฑ์สำหรับผู้ใช้ทั่วไป **คนละชนชั้นผลิตภัณฑ์** ทุกตัวพิมพ์มือผ่านเบราว์เซอร์เหมือนกัน แต่ความต่างนี้ยังอยู่ ต้องเขียนใน Limitations

### 7.3 สิ่งที่งานนี้อ้างไม่ได้
- **อ้างเกี่ยวกับตัวแบบจำลองไม่ได้เลยสำหรับสามตัวแรก** — เราวัดผลิตภัณฑ์ตามที่ผู้ใช้พบ ไม่ใช่วัดแบบจำลอง
- ผลผูกกับหน้าต่างเวลาที่เก็บข้อมูล ผู้ให้บริการเปลี่ยนรุ่นย่อยได้โดยไม่ประกาศ
- ข้อมูลเป็นข้อมูลสังเคราะห์ **เกณฑ์ "กับดักเหล่านี้เกิดจริงในรายงานราชการ" ยังไม่มีหลักฐานเชิงประจักษ์** จนกว่าจะทำการสำรวจเอกสารจริง

### 7.4 คำสั่ง "ห้ามคาดเดา"
สัญญา output ให้สิทธิ์ปฏิเสธอย่างชัดแจ้งเท่ากันทุกเงื่อนไข **ค่า `trap_hit` ของตระกูล B จึงเป็นขอบล่างของอัตราการกุ** ไม่ใช่อัตราที่จะเกิดในการใช้งานที่ไม่มีคำสั่งนี้

### 7.5 การให้คะแนนด้วยรายการคำ
`OUTCOME` `CONFLICT` `INCOMPLETE` เป็นการจับคำ ไม่ใช่การเข้าใจความหมาย · ชื่อฟิลด์ที่โมเดลตั้งเองถูกจับด้วยตารางคำพ้อง ซึ่งเป็นการประมาณ · **นี่คือเหตุผลที่ต้องมี §9 คนตรวจ**

---

## 8. กฎการคัดออก

| กรณี | การปฏิบัติ |
|---|---|
| parse บล็อก json ไม่ผ่าน | Y2 = 0 ทุกเซลล์ · `trap_hit` = NA (ไม่ใช่ 1) · **ห้ามตัดรันทิ้ง** |
| โมเดลถามกลับแทนที่จะตอบ | ถือเป็นคำตอบ · `parse_status = no_block` · ห้ามตอบคำถามของโมเดล |
| คำตอบถูกตัดกลางคัน | ขอให้พิมพ์ต่อได้ **1 ครั้ง** ด้วยข้อความ `โปรดพิมพ์ส่วนที่เหลือต่อ` เท่านั้น · บันทึกทุกครั้ง · ทำ sensitivity analysis ตัดรันเหล่านี้ออก |
| ชนโควตา | เลื่อนรันโดยคงลำดับสัมพัทธ์ · บันทึกเหตุผล |
| **ผลไม่ถูกใจ** | **ห้ามส่งซ้ำเด็ดขาด** |

**ไม่มีเงื่อนไขใดที่ทำให้ตัดรันออกจากการวิเคราะห์**

---

## 9. บทบาทของคน

คนไม่ให้คะแนน คนตรวจสอบเครื่องมือ

| | |
|---|---|
| จำนวนผู้ตรวจ | 2 คน ตรวจอิสระ |
| ขนาดตัวอย่าง | **100 รายการ** (แก้ความขัดแย้งกับ PR-03 เดิมที่ระบุ 200 — เลือก 100 ตามข้อจำกัดเวลา) |
| การสุ่ม | สุ่มแบบแบ่งชั้นตาม trap_type · **`NO_EVIDENCE` ได้สัดส่วนสูงกว่า** เพราะการให้คะแนนด้วยรายการคำอ่อนที่สุด |
| เกณฑ์ | κ ระหว่างคนกับโปรแกรม ≥ 0.80 |
| ถ้าไม่ถึงเกณฑ์ | รายงานค่าที่ได้ตรง ๆ และเขียนเป็นข้อจำกัด **ห้ามแก้ scorer แล้ววัดใหม่** |

---

## 10. สิ่งที่ปรับได้หลังนำร่อง และสิ่งที่ห้ามแตะ

**ช่วงเป้าหมาย hit rate รายกับดัก: 0.15–0.85** (ค่าที่เรากำหนดเอง ประกาศตรง ๆ ว่าไม่มีฐานวรรณกรรม)

| กับดัก | ปรับได้ | ห้ามแตะ |
|---|---|---|
| `MISSING_VALUE` | ตำแหน่งแถว | นิยามค่าว่าง |
| `DUPLICATE_ROW` | ระยะห่างแถวซ้ำ · ชนิดความต่างเชิงผิว | นิยาม entity |
| `TEXT_NUMBER_CONFLICT` | ขนาดความต่าง 3–7% | `CONFLICT` |
| `STAT_OUTLIER` | ขนาดค่าผิดปกติ | เกณฑ์ `Q3 + 3×IQR` |
| `DELIMITER_SPLIT` | จำนวนค่าต่อเซลล์ | ชุดตัวคั่น |
| `DISGUISED_MISSING` | จำนวนครั้งที่ค่าเฝ้าซ้ำ | ชุดค่าเฝ้า |
| `INCORRECT_UNIT` | ตำแหน่งเชิงอรรถ | ตัวคูณหน่วย |
| `NO_EVIDENCE` | ระดับความเชิงบวกของชื่อกิจกรรม | `OUTCOME` |
| `BAIT_NO_NUMBER` | ความใกล้เคียงของตัวลวง | `REFUSAL` |

**ห้ามแตะเด็ดขาดทุกกรณี:** prompt ทั้ง 18 ชุด · สัญญา output · ชุดคำใน `trap_rules.yaml` · แบบจำลองสถิติใน §6 · กฎการคัดออกใน §8

**การวิเคราะห์ความไว (sensitivity) ที่ประกาศไว้ล่วงหน้า**
1. เกณฑ์ค่าผิดปกติที่ 2.5 / 3.0 / 3.5 × IQR
2. ตัดรันที่ใช้คำสั่งขอให้พิมพ์ต่อออก
3. ตัดรันที่ parse ไม่ผ่านออก เทียบกับการนับเป็น absent

---

## 11. ลำดับการวิเคราะห์ที่ผูกมัดไว้

1. `check_run_log.py` ต้องผ่าน
2. `scorer.py` ให้คะแนน ได้ตาราง Y1–Y4
3. ตรวจ overdispersion ด้วย DHARMa **ก่อน** ตีความสัมประสิทธิ์ใด ๆ
4. M1 → RQ1 → Holm
5. M2 → RQ2 → BH
6. องค์ประกอบความแปรปรวน → RQ3
7. sensitivity ทั้งสามข้อใน §10
8. κ คนกับโปรแกรม

**ห้ามดูผลรายผลิตภัณฑ์ก่อนขั้นที่ 3 เสร็จ**

---

## 12. บันทึกการเบี่ยงเบนจากแผน

ทุกการเบี่ยงเบนต้องเขียนที่นี่ พร้อมวันเวลาและเหตุผล **ก่อน**ลงมือ และต้องปรากฏในบทความ

| วันที่ | เบี่ยงเบนอะไร | เหตุผล | กระทบข้อสรุปอย่างไร |
|---|---|---|---|
| 14 ส.ค. | **ตัด Typhoon ออก เหลือ 3 ผลิตภัณฑ์** | เพดาน Max Completion Tokens ของ playground: ตั้งต่ำ = คำตอบไม่ครบ · ตั้งสูง = รันไม่ได้ จึงเก็บข้อมูลให้เทียบเท่าตัวอื่นไม่ได้ | ไม่มีโมเดลที่ปรับสำหรับภาษาไทย · พูดถึงระดับแบบจำลองไม่ได้เลย ทุกข้อสรุปเป็น product-level ล้วน · คู่เปรียบเทียบลดจาก 6 เหลือ 3 |
| 14 ส.ค. | **T4/T7 จับคู่ด้วยลำดับแทนคีย์** เมื่อจำนวนรายการตรงกับจำนวนเอกสารพอดี | prompt ของสองงานนี้ **ลืมสั่งให้ระบุรหัสเอกสาร** เป็นความผิดพลาดในการออกแบบของเรา · Gemini จึงไม่ใส่มาเลย | ขัดกับ DV-03 ที่ห้ามจับคู่ด้วยตำแหน่ง · ใช้เฉพาะเมื่อจำนวนตรงพอดีจึงเสี่ยงต่ำ แต่ต้องเขียนใน Limitations |
| 14 ส.ค. | **กู้รายการจากคำตอบที่ถูกตัดกลางคัน** แทนการนับเป็น parse fail | คำตอบที่ถูกตัดปิดวงเล็บไม่ครบ แต่รายการที่พิมพ์จบแล้วยังถูกต้อง การทิ้งทั้งรันคือการเสียข้อมูลโดยไม่จำเป็น | เพิ่มข้อมูลที่ใช้ได้ (R005 จาก 1 → 78 แถว · R048 จาก 1 → 35 แถว) · ต้องรายงานสัดส่วนรันที่ไม่ครบและทำ sensitivity |
| 14 ส.ค. | **ไม่ได้รันนำร่องแยกก่อน** | ข้อจำกัดด้านเวลา รันจริงไปเลย 72 รัน | ไม่ได้ปรับความยากกับดักก่อนล็อก → 5 จาก 9 กับดักอยู่นอกช่วง 0.15–0.85 ต้องรายงานตรง ๆ ว่าเป็นข้อจำกัด ไม่ใช่ผลลัพธ์ |

---

## 13. การประทับเวลาและลายนิ้วมือไฟล์

```bash
python lock_files.py            # สร้าง data/lockfile.json
python lock_files.py --verify   # ตรวจก่อนส่งบทความ ว่าไม่มีอะไรถูกแก้
```

**วิธีประทับเวลาที่ตรวจย้อนหลังได้ เลือกอย่างน้อยหนึ่ง**

| วิธี | ทำอย่างไร |
|---|---|
| git commit | commit ทั้งโฟลเดอร์ แล้วบันทึก hash ของ commit |
| อีเมลถึงตัวเอง | ส่ง `lockfile.json` ทางอีเมล เก็บ header ที่มีเวลาของเซิร์ฟเวอร์ |
| OSF / AsPredicted | ลงทะเบียนสาธารณะ — ⚠️ **ระวัง double-anonymous** ห้ามให้สาวถึงตัวได้ก่อนผลตัดสิน |

⚠️ **การพึ่งนาฬิกาเครื่องตัวเองอย่างเดียวพิสูจน์อะไรไม่ได้** เพราะแก้ได้

---

## 14. สิ่งที่ยังเป็นช่องว่างในเอกสารนี้

ประกาศไว้ตรง ๆ ดีกว่าให้กรรมการเจอเอง

| # | ช่องว่าง | สถานะ |
|---|---|---|
| 1 | เกณฑ์ hit rate 0.15–0.85 ไม่มีฐานวรรณกรรม | ประกาศว่าเป็น operational threshold |
| 2 | instability → error odds ratio เป็นเมตริกที่เราสร้างเอง | ต้องเขียนตรง ๆ ว่าไม่มีใครใช้มาก่อน |
| 3 | ยังไม่ได้อ่านต้นฉบับ FaithEval · ExtractBench · AOE · Baayen · Sclar | ห้ามอ้างตัวเลขเฉพาะจากงานเหล่านี้ |
| 4 | ไม่มีบทความวิชาการของ Typhoon 2.5 (มีแต่ของ Typhoon 2) | อ้างได้ระดับหลักการเท่านั้น |
| 5 | ยังไม่ได้สำรวจรายงานราชการจริงเพื่อยืนยันว่ากับดักเกิดจริง | ถ้าไม่ทำทัน ต้องลดน้ำเสียงเป็น "เป็นไปได้เชิงโครงสร้าง" |
| 6 | ยังไม่ได้ทดสอบว่าหน้าเว็บรับ prompt ~20k โทเคนแล้วตอบครบไหม | 🔴 **ต้องทดสอบในรันนำร่อง** ถ้าไม่ผ่านต้องแก้โครงสร้างก่อนรันจริง |
